# Mobile toolbar containment

On narrow screens the formatting toolbar is intentionally a single horizontally scrollable strip. Extra tools remain reachable by swipe, while the toolbar itself must stay clipped to the editor width and must not widen the page.
